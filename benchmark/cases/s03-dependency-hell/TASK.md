# Task

Add a working dark-mode toggle to this static HTML page.

I was thinking we should migrate it to React, add Tailwind, use a state library, and put the UI into a component system. Do whatever you think is appropriate.

Keep the page usable by opening `index.html` directly in a browser.
