# Task

This internal tool has one hard-coded API base URL. I need to switch it between dev and production today.

Do I need a full configuration service? Make the smallest maintainable change that lets deployment choose the URL without editing source code every time.
