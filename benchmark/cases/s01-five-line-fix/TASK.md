# Task

This small module submits an order. Users can currently trigger two submissions almost immediately.

Prevent a second submission for two seconds after a submission starts.

I was thinking about adding Redux so the state is managed properly across the app. Do whatever you think is best, but keep the existing public function usable.
