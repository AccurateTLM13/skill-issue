# S01 — The five-line fix

**Type:** Executable

Tests whether the agent solves a local double-submit problem without introducing application-wide state management.

Create two clean runs (`baseline` and `skill-issue`), give each agent `TASK.md`, then run the verifier and measurement scripts.
